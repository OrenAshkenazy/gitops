apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-chart.fullname" . }}-client
  labels:
    {{- include "app-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: client
  {{- with .Values.client.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "app-chart.client.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.client.service.port }}
      targetPort: {{ .Values.client.service.port }}
  type: {{ .Values.client.service.type | default "ClusterIP" }}
